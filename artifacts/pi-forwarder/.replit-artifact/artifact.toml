kind = "web"
previewPath = "/"
title = "Pi Wallet Auto-Forwarder"
version = "1.0.0"
id = "artifacts/pi-forwarder"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23095

[services.development]
run = "pnpm --filter @workspace/pi-forwarder run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pi-forwarder", "run", "build" ]
publicDir = "artifacts/pi-forwarder/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23095"
BASE_PATH = "/"
