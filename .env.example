# Required — PostgreSQL connection string
# On Render this is injected automatically from the linked database.
# For local development it is managed by Replit automatically.
DATABASE_URL=postgres://user:password@host:5432/pi_forwarder

# Required — random secret used to sign sessions
# Generate with: openssl rand -hex 32
SESSION_SECRET=replace-me-with-a-random-secret
